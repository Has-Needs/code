import { createRequire } from 'node:module';const require = createRequire(import.meta.url);
import {
  registerSignalsRuntime
} from "./chunk-3CVNPFPR.mjs";

// packages/core/echo/echo-signals/src/preact.ts
import { signal, batch, untracked } from "@preact/signals";
var registered = false;
var registerSignalsRuntime2 = () => {
  if (registered) {
    return false;
  }
  registerSignalsRuntime({
    createSignal: (debugInfo) => {
      const thisSignal = signal({});
      thisSignal.__debugInfo = debugInfo;
      return {
        notifyRead: () => {
          const _ = thisSignal.value;
        },
        notifyWrite: () => {
          thisSignal.value = {};
        }
      };
    },
    batch,
    untracked
  });
  registered = true;
  return true;
};
export {
  registerSignalsRuntime2 as registerSignalsRuntime
};
//# sourceMappingURL=index.mjs.map
