/**
 * Utility to automatically log debug info.
 *
 * ```
 *   // Called via `console.log`.
 *   [inspect.custom] () {
 *     return inspectObject(this);
 *   }
 *
 *   // Called via `JSON.stringify`.
 *   toJSON () {
 *     return { ... };
 *   }
 * ```
 */
export declare const inspectObject: (obj: any) => string;
//# sourceMappingURL=inspect.d.ts.map