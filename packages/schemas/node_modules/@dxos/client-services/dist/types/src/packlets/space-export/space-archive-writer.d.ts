import { Resource, type Context } from '@dxos/context';
import type { IdentityDid, SpaceId } from '@dxos/keys';
import type { SpaceArchive } from '@dxos/protocols/proto/dxos/client/services';
export type SpaceArchiveBeginProps = {
    spaceId?: SpaceId;
    /**
     * DID of the identity that exported the space.
     */
    exportedBy?: IdentityDid;
};
export declare class SpaceArchiveWriter extends Resource {
    private _tar?;
    private _archive?;
    private _meta?;
    private _currentRootUrl?;
    protected _open(ctx: Context): Promise<void>;
    protected _close(): Promise<void>;
    begin(meta: SpaceArchiveBeginProps): Promise<void>;
    setCurrentRootUrl(url: string): Promise<void>;
    writeDocument(documentId: string, data: Uint8Array): Promise<void>;
    finish(): Promise<SpaceArchive>;
}
//# sourceMappingURL=space-archive-writer.d.ts.map