import type { Message } from '../signal-methods';
export declare class SignalClientMonitor {
    private readonly _performance;
    /**
     * Timestamp of when the connection attempt was began.
     */
    private _connectionStarted;
    /**
     * Timestamp of last state change.
     */
    private _lastStateChange;
    getRecordedTimestamps(): {
        connectionStarted: Date;
        lastStateChange: Date;
    };
    recordStateChangeTime(): void;
    recordConnectionStartTime(): void;
    recordReconnect(params: {
        success: boolean;
    }): void;
    recordJoin(): void;
    recordLeave(): void;
    recordMessageReceived(message: Message): void;
    recordMessageSending(message: Message, sendMessage: () => Promise<void>): Promise<void>;
    recordStreamCloseErrors(count: number): void;
    recordReconciliation(params: {
        success: boolean;
    }): void;
}
//# sourceMappingURL=signal-client-monitor.d.ts.map