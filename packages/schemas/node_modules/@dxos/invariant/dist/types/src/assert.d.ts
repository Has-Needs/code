/**
 * Use this to assert the arguments of a function.
 *
 * @example
 * ```ts
 * function foo(arg: string) {
 *   assertArgument(typeof arg === 'string', 'arg must be a string');
 * }
 * ```
 */
export declare const assertArgument: (condition: unknown, message: string) => asserts condition;
/**
 * Use this to assert the state of an object.
 *
 * @example
 * ```ts
 * class Foo {
 *   private _bar: boolean;
 *
 *   public get bar() {
 *     assertState(this._bar, 'bar must be true');
 *     return this._bar;
 *   }
 * }
 *
 * const foo = new Foo();
 * foo.bar = false; // This will throw an InvalidStateError
 * ```
 */
export declare const assertState: (condition: unknown, message: string) => asserts condition;
export declare class InvalidStateError extends Error {
}
//# sourceMappingURL=assert.d.ts.map