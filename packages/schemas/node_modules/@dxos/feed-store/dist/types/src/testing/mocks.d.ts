import { Event } from '@dxos/async';
import { PublicKey } from '@dxos/keys';
import type { FeedWriter, WriteOptions, WriteReceipt } from '../feed-writer';
/**
 * Mock writer collects and emits messages.
 */
export declare class MockFeedWriter<T extends {}> implements FeedWriter<T> {
    readonly feedKey: PublicKey;
    readonly written: Event<[T, WriteReceipt]>;
    readonly messages: T[];
    constructor(feedKey?: PublicKey);
    write(data: T, { afterWrite }?: WriteOptions): Promise<WriteReceipt>;
}
//# sourceMappingURL=mocks.d.ts.map