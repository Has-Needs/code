import { type RpcPort } from '@dxos/rpc';
import { type IFramePortOptions, type WorkerPortOptions } from './ports';
/**
 * Facilitates the multiplexing of multiple RpcPorts over a single MessagePort.
 */
export declare class PortMuxer {
    private readonly _messagePort?;
    private readonly _activeChannels;
    private readonly _rpcPorts;
    constructor(_messagePort?: MessagePort | undefined);
    createWorkerPort(options: Omit<WorkerPortOptions, 'port' | 'subscribe' | 'channel'> & {
        channel: string;
    }): RpcPort;
    createIFramePort(options: IFramePortOptions): RpcPort;
    private onWorkerMessage;
    private onWindowMessage;
}
//# sourceMappingURL=port-muxer.d.ts.map