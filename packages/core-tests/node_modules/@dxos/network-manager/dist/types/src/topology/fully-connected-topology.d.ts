import { type PublicKey } from '@dxos/keys';
import { type SwarmController, type Topology } from './topology';
export declare class FullyConnectedTopology implements Topology {
    private _controller?;
    toString(): string;
    init(controller: SwarmController): void;
    update(): void;
    onOffer(peer: PublicKey): Promise<boolean>;
    destroy(): Promise<void>;
}
//# sourceMappingURL=fully-connected-topology.d.ts.map