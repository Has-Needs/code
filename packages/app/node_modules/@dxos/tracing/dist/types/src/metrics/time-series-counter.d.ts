import { type Metric } from '@dxos/protocols/proto/dxos/tracing';
import { BaseCounter } from './base';
export declare class TimeSeriesCounter extends BaseCounter {
    private _currentValue;
    private _totalValue;
    private _buckets;
    units?: string;
    constructor({ units }?: {
        units?: string;
    });
    inc(by?: number): void;
    _tick(time: number): void;
    getData(): Metric;
}
//# sourceMappingURL=time-series-counter.d.ts.map