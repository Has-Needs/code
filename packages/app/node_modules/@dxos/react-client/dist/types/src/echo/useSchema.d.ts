import { type Schema } from 'effect';
import { type Client } from '@dxos/client';
import { type Space } from '@dxos/client/echo';
/**
 * Subscribe to and retrieve schema changes from a space's schema registry.
 */
export declare const useSchema: (client: Client, space: Space | undefined, typename: string | undefined) => Schema.Schema.AnyNoContext | undefined;
//# sourceMappingURL=useSchema.d.ts.map