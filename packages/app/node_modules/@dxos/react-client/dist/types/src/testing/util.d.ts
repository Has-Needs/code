import React from 'react';
import { Client, type Config } from '@dxos/client';
import { type Space } from '@dxos/client/echo';
export declare const createClient: ({ config, createIdentity, createSpace, }?: {
    config?: Config;
    createIdentity?: boolean;
    createSpace?: boolean;
}) => Promise<{
    client: Client;
    space: Space | undefined;
}>;
export declare const createClientContextProvider: (client: Client) => Promise<({ children }: any) => React.JSX.Element>;
export declare const createClientProvider: (client: Client) => Promise<({ children }: any) => React.JSX.Element>;
//# sourceMappingURL=util.d.ts.map