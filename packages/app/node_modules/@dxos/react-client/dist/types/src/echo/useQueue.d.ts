import { type Queue } from '@dxos/client/echo';
import { type Obj, type Relation } from '@dxos/echo';
import { type DXN } from '@dxos/keys';
export type UseQueueOptions = {
    pollInterval?: number;
};
/**
 * Polls the given Edge queue.
 */
export declare const useQueue: <T extends Obj.Any | Relation.Any>(queueDxn?: DXN, options?: UseQueueOptions) => Queue<T> | undefined;
//# sourceMappingURL=useQueue.d.ts.map