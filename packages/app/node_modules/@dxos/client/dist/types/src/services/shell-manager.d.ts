import { Event } from '@dxos/async';
import { type AppContextRequest, type LayoutRequest, ShellDisplay, type InvitationUrlRequest } from '@dxos/protocols/proto/dxos/iframe';
import { type IFrameManager } from './iframe-manager';
/**
 * Provide access to the shell via RPC connection.
 */
export declare class ShellManager {
    private readonly _iframeManager;
    private readonly _channel;
    readonly contextUpdate: Event<AppContextRequest>;
    private _shellRpc?;
    private _display;
    constructor(_iframeManager: IFrameManager, _channel?: string);
    get display(): ShellDisplay;
    setLayout(request: LayoutRequest): Promise<void>;
    setInvitationUrl(request: InvitationUrlRequest): Promise<void>;
    open(): Promise<void>;
    close(): Promise<void>;
}
//# sourceMappingURL=shell-manager.d.ts.map