import { Event } from '@dxos/async';
import { type ClientServices, type ClientServicesProvider } from '@dxos/client-protocol';
import { type ProtoRpcPeer, type RpcPort } from '@dxos/rpc';
/**
 * Implements services that are not local to the app.
 * For example, the services can be located in Wallet Extension.
 */
export declare class ClientServicesProxy implements ClientServicesProvider {
    private readonly _port;
    private readonly _timeout;
    readonly closed: Event<Error | undefined>;
    private _proxy?;
    constructor(_port: RpcPort, _timeout?: number);
    get proxy(): ProtoRpcPeer<ClientServices>;
    get descriptors(): import("@dxos/rpc").ServiceBundle<ClientServices>;
    get services(): ClientServices;
    open(): Promise<void>;
    close(): Promise<void>;
}
//# sourceMappingURL=service-proxy.d.ts.map