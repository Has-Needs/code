import { type PublicKey } from '@dxos/keys';
import { type Credential, type ProfileDocument } from '@dxos/protocols/proto/dxos/halo/credentials';
import { type CredentialProcessor } from './credential-processor';
export type ProfileStateMachineParams = {
    identityKey: PublicKey;
    onUpdate?: () => void;
};
/**
 * Processes device invitation credentials.
 */
export declare class ProfileStateMachine implements CredentialProcessor {
    private readonly _params;
    profile?: ProfileDocument;
    constructor(_params: ProfileStateMachineParams);
    processCredential(credential: Credential): Promise<void>;
}
//# sourceMappingURL=profile-state-machine.d.ts.map