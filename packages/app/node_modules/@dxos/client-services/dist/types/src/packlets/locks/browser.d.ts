import { type ResourceLock, type ResourceLockOptions } from './resource-lock';
export declare class Lock implements ResourceLock {
    private readonly _broadcastChannel;
    private readonly _lockKey;
    private readonly _onAcquire;
    private readonly _onRelease;
    private _releaseTrigger;
    constructor({ lockKey, onAcquire, onRelease }: ResourceLockOptions);
    get lockKey(): string;
    acquire(): Promise<void>;
    release(): Promise<void>;
    private _onMessage;
    private _requestLock;
}
export declare const isLocked: (lockPath: string) => never;
//# sourceMappingURL=browser.d.ts.map