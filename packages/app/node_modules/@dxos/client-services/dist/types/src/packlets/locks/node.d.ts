import { type ResourceLock, type ResourceLockOptions } from './resource-lock';
export declare class Lock implements ResourceLock {
    private readonly _lockPath;
    private readonly _onAcquire;
    private readonly _onRelease;
    private _fileHandle?;
    constructor({ lockKey: lockPath, onAcquire, onRelease }: ResourceLockOptions);
    get lockKey(): string;
    acquire(): Promise<void>;
    release(): Promise<void>;
}
export declare const isLocked: (lockPath: string) => Promise<boolean>;
//# sourceMappingURL=node.d.ts.map