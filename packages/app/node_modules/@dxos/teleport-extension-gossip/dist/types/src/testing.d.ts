import { PublicKey } from '@dxos/keys';
import { type TestConnection, TestPeer as TestPeerBase } from '@dxos/teleport/testing';
import { Gossip } from './gossip';
import { Presence } from './presence';
export type TestAgentOptions = {
    peerId?: PublicKey;
    announceInterval?: number;
    offlineTimeout?: number;
};
export declare class TestAgent extends TestPeerBase {
    readonly gossip: Gossip;
    readonly presence: Presence;
    constructor({ peerId, announceInterval, offlineTimeout }?: TestAgentOptions);
    onOpen(connection: TestConnection): Promise<void>;
    waitForAgentsOnline(agents: TestAgent[], timeout?: number): Promise<void>;
    destroy(): Promise<void>;
}
//# sourceMappingURL=testing.d.ts.map