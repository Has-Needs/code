import { type RandomAccessStorage } from 'random-access-storage';
import { AbstractStorage, type DiskInfo, type Storage, StorageType } from '../common';
/**
 * Storage interface implementation for Node.
 */
export declare class NodeStorage extends AbstractStorage implements Storage {
    type: StorageType;
    private _initialized;
    private _loadFiles;
    protected _createFile(path: string, filename: string, opts?: any): RandomAccessStorage;
    protected _destroy(): Promise<void>;
    protected _getFiles(path: string): Promise<Map<string, import(".").File>>;
    getDiskInfo(): Promise<DiskInfo>;
}
//# sourceMappingURL=node-storage.d.ts.map