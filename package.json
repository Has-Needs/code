{
  "name": "has-needs-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/app",
    "packages/schemas",
    "packages/core-tests",
    "packages/core-logic"
  ],
  "scripts": {
    "start": "npm start --workspace=packages/app",
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "dependencies": {
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.1"
  }
}
