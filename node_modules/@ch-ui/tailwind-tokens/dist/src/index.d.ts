import { OptionalConfig, CustomThemeConfig as TwTheme } from 'tailwindcss/types/config';
import { TokenSet } from '@ch-ui/tokens';
type TwKey = keyof TwTheme;
export type TailwindAdapterFacet = {
    facet: string;
    disposition?: 'extend' | 'overwrite';
    tokenization?: 'keep-series' | 'omit-series' | 'recursive';
    seriesValueSeparator?: string;
};
export type TailwindAdapterConfig = Partial<Record<TwKey, TailwindAdapterFacet>>;
declare const _default: (tokensConfig: TokenSet, adapterConfig?: TailwindAdapterConfig) => OptionalConfig['theme'];
export default _default;
