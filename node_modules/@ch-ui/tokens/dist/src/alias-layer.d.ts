import { AliasLayer } from './types';
export declare const renderAliasLayer: <K extends string = string, Q extends string = string>({ conditions, aliases, namespace, semanticNamespace, }: AliasLayer<K, Q>) => string;
