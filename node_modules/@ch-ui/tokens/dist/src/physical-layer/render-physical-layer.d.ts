import { Definitions, InvariantCheck, PhysicalLayer, RenderTokens, SemanticValues, Series } from '../types';
export declare const renderPhysicalLayer: <L extends PhysicalLayer<string, Series<any>>, S extends Series<any>, ResolvedSeries extends S = S>({ conditions, series, namespace, definitions: layerDefinitions }: L, renderTokens: RenderTokens<ResolvedSeries>, invariantCheck: InvariantCheck, semanticValues?: SemanticValues, ...ancestorDefinitions: Definitions[]) => string;
