import { AuditOptions, AuditTokens, Definitions, PhysicalLayer, SemanticValues, Series, TokenAudit } from '../types';
export declare const auditPhysicalLayer: <L extends PhysicalLayer<string, Series<any>>, S extends Series<any>>({ series, namespace, definitions: layerDefinitions }: L, { condition }: AuditOptions, auditTokens: AuditTokens<S>, semanticValues?: SemanticValues, ...ancestorDefinitions: Definitions[]) => Record<string, TokenAudit[]>;
