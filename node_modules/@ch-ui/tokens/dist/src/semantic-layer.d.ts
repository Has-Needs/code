import { SemanticLayer } from './types';
export declare const renderSemanticLayer: <K extends string = string, P extends string = string, V = number, Q extends string = string>({ conditions, sememes, namespace, physicalNamespace, }: SemanticLayer<K, P, V, Q>) => string;
