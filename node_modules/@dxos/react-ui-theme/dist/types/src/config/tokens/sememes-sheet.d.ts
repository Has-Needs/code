export declare const sheetSememes: {
    axisSurface: {
        light: [string, number];
        dark: [string, number];
    };
    axisText: {
        light: [string, number];
        dark: [string, number];
    };
    axisSelectedSurface: {
        light: [string, number];
        dark: [string, number];
    };
    axisSelectedText: {
        light: [string, number];
        dark: [string, number];
    };
    gridCell: {
        light: [string, string];
        dark: [string, number];
    };
    gridCellSelected: {
        light: [string, number];
        dark: [string, number];
    };
    gridOverlay: {
        light: [string, string];
        dark: [string, string];
    };
    gridSelectionOverlay: {
        light: [string, string];
        dark: [string, string];
    };
    gridHighlight: {
        light: [string, string];
        dark: [string, string];
    };
    gridCommented: {
        light: [string, number];
        dark: [string, number];
    };
    gridCommentedActive: {
        light: [string, string];
        dark: [string, string];
    };
};
export declare const sheetAliases: {
    activeSurface: {
        root: string[];
    };
};
//# sourceMappingURL=sememes-sheet.d.ts.map