export declare const codemirrorSememes: {
    cmCodeblock: {
        light: [string, string];
        dark: [string, string];
    };
    cmActiveLine: {
        light: [string, string];
        dark: [string, string];
    };
    cmSeparator: {
        light: [string, number];
        dark: [string, number];
    };
    cmCursor: {
        light: [string, number];
        dark: [string, number];
    };
    cmSelection: {
        light: [string, string];
        dark: [string, string];
    };
    cmFocusedSelection: {
        light: [string, number];
        dark: [string, number];
    };
    cmHighlight: {
        light: [string, number];
        dark: [string, number];
    };
    cmHighlightSurface: {
        light: [string, number];
        dark: [string, number];
    };
    cmComment: {
        light: [string, number];
        dark: [string, number];
    };
    cmCommentSurface: {
        light: [string, number];
        dark: [string, number];
    };
};
//# sourceMappingURL=sememes-codemirror.d.ts.map