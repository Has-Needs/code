import { Duplex } from 'node:stream';
/**
 * Duplex stream for testing.
 * If this stream is piped into another stream, use `push` to send data, and `assertReceivedAsync` to assert the received data.
 */
export declare class TestStream extends Duplex {
    static assertConnectivity(stream1: TestStream, stream2: TestStream, { timeout }?: {
        timeout?: number;
    }): Promise<void>;
    private _received;
    private _onWrite;
    _write(chunk: any, encoding: BufferEncoding, callback: (error?: Error | null) => void): void;
    _read(size: number): void;
    assertReceivedAsync(data: Buffer | string, { timeout }?: {
        timeout?: number;
    }): Promise<void>;
}
//# sourceMappingURL=test-stream.d.ts.map