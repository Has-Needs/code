import { StackTrace } from '@dxos/debug';
type OpenResource = {
    name: string;
    openStack: StackTrace;
};
export declare const trackResource: (resourceProvider: () => OpenResource) => (() => void);
/**
 * Makes sure that the resource is at the end of the test.
 *
 * Example:
 *
 * ```typescript
 * @trackLeaks('open', 'close')
 * class Resource {}
 * ```
 */
export declare const trackLeaks: (open: string, close: string) => ClassDecorator;
export declare const dumpLeaks: () => void;
export {};
//# sourceMappingURL=track-leaks.d.ts.map