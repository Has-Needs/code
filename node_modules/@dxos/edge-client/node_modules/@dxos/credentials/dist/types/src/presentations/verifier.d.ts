import { type Presentation, type Proof } from '@dxos/protocols/proto/dxos/halo/credentials';
import { type VerificationResult } from '../credentials';
export declare const verifyPresentation: (presentation: Presentation) => Promise<VerificationResult>;
export declare const verifyPresentationChain: (presentation: Presentation, proof: Proof) => Promise<VerificationResult>;
/**
 * Verifies that the signature is valid and was made by the signer.
 * Does not validate other semantics (e.g. chains).
 */
export declare const verifyPresentationSignature: (presentation: Presentation, proof: Proof) => Promise<VerificationResult>;
//# sourceMappingURL=verifier.d.ts.map