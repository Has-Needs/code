import { type Metric } from '@dxos/protocols/proto/dxos/tracing';
import { BaseCounter } from './base';
export declare class TimeUsageCounter extends BaseCounter {
    private _currentValue;
    private _totalValue;
    private _buckets;
    private _lastTickTime;
    record(time: number): void;
    beginRecording(): {
        end: () => void;
    };
    _tick(time: number): void;
    getData(): Metric;
}
//# sourceMappingURL=time-usage-counter.d.ts.map