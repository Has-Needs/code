'use strict';

const signalsCore = require('@preact/signals-core');
const react = require('react');

const Empty = [];
function useSignal(value) {
  return react.useMemo(() => signalsCore.signal(value), Empty);
}
function useComputed(compute) {
  const $compute = react.useRef(compute);
  $compute.current = compute;
  return react.useMemo(() => signalsCore.computed(() => $compute.current()), Empty);
}
function useSignalEffect(cb) {
  const callback = react.useRef(cb);
  callback.current = cb;
  react.useEffect(() => {
    return signalsCore.effect(() => callback.current());
  }, Empty);
}

exports.useComputed = useComputed;
exports.useSignal = useSignal;
exports.useSignalEffect = useSignalEffect;
//# sourceMappingURL=hooks.cjs.map
