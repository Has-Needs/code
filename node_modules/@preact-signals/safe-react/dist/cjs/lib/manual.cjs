'use strict';

const tracking = require('./tracking.cjs');
const reactFastHoc = require('react-fast-hoc');

const withTrackSignals = reactFastHoc.wrapIntoProxy(
  new reactFastHoc.RewriteCall((props) => {
    const tracker = tracking.useSignals();
    try {
      return props.renderComponent(...props.args);
    } finally {
      tracker.f();
    }
  })
);

exports.withTrackSignals = withTrackSignals;
//# sourceMappingURL=manual.cjs.map
